/* how to use the switch keyword */

/*
var courseMarks = 70
switch (courseMarks) {
    // if the marks entered is 0
    case 0:
        console.log("Sorry!, you failed")
        break;
    case 50:
        console.log("Your grade is \t" + D)  /* D is a certain value *
        break;

    default:
        break;
}
*/

var marks = 0
switch (marks >= 49) {
    case true:
        console.log("You did not read enough")
        break;
}

/* this witch statement is for finding the range */
switch (marks >= 50 && marks <= 60) {
    case value:
        
        break;

    default:
        break;
}
