
function validate() {
    let userId = document.getElementById('usersId')
    
    if (userId.value == '') {
        alert('ID must be filled ')

    } else if (userId.value.length > 7 && userId.value.length <= 15) {
        alert('this')

    }
    
    }
function password(){
    
}